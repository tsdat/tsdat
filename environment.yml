name: tsdat
channels:
  - arm-doe
  - conda-forge
  - defaults
dependencies:
  - python>=3.10,<3.14  # adi_py C extensions are incompatible with the free-threading Python 3.14 build from conda-forge
  - adi_py>=4.0.0
  - hdf5
  - h5py
