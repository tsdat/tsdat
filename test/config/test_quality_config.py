# TEST: CheckerConfig, HandlerConfig require classname, set default params (1 test each)
# TEST: ManagerConfig produces expected model (for valid input)
# TEST: ManagerConfig requires name, checker, at least one handler, at least one apply_to entry
# TEST: QualityDefinition does not have to have any managers
# TEST: QualityDefinition managers must have unique names
# TEST: QualityDefinition produces expected model (for valid input)
# TEST: QualityDefinition produces expected model (for 'from_yaml' method)
