# TEST: DataHandler requires name, classname
# TEST: StorageDefinition InputHandlers can be instantiated without regex if only one handler (regex set to .*)
# TEST: StorageDefinition OutputHandlers can be instantiated without regex if only one handler (regex set to .*)
# TEST: StorageDefinition InputHandlers require regex for len >= 2, for each DataHandler
# TEST: StorageDefinition OutputHandlers require regex for len >= 2, for each DataHandler
# TEST: StorageDefiniton InputHandler names must be unique
# TEST: StorageDefiniton OutputHandler names must be unique
# TEST: StorageDefinition produces expected model (for valid input)
# TEST: StorageDefinition produces expected model (for 'from_yaml' method)
